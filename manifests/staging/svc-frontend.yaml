apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: staging
spec:
  selector:
    app: frontend
    color: blue
  ports:
  - port: 80
    targetPort: 80
  type: ClusterIP
